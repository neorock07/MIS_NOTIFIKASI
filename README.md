# Push Notification for MIS Division

Trigger if any ESS service order to each person in MIS division.
There are two types of program, here are for `windows` version and `linux` version.

This program using Python.